/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ["cdn.dummyjson.com", "cdn.corporatefinanceinstitute.com"], // ✅ add this
  },
};

export default nextConfig;
